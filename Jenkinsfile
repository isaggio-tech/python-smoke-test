// Jenkinsfile (Declarative Pipeline) //
pipeline {
    agent { label 'python' }
    stages {
        stage('Version Check') {
            steps {
                sh 'python --version'
            }
        }
    }
}
